# Chat backend rewrite target (build-time; used by next.config.ts).
# Same host as dam-frontend VITE_API_URL.
CHAT_API_URL=https://164-92-131-93.sslip.io

# Browser Socket.io host only (HTTP chat stays same-origin via /chat rewrites).
NEXT_PUBLIC_CHAT_API_URL=https://164-92-131-93.sslip.io
